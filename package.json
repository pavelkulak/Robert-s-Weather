{
  "name": "robert_s_weather",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.5.3",
    "vite": "^6.2.0"
  },
  "lint-staged": {
    "src/**/*.{js,ts,vue}": [
      "prettier --write"
    ]
  }
}
